import React from "react";
import { Text, StyleSheet } from "react-native";

const NavigateToScreen = () => {
  return <Text style={styles.text}>Navigate To Screen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default NavigateToScreen;
